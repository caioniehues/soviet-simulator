# Validation allow-list for RADV NAVI32 (sov-y27, sov-ejz).
#
# Adapter scope: the gate applies these entries ONLY to runs whose capture
# record matches every field below, and refuses to apply them elsewhere.
# adapter_match: {"backend": "Vulkan", "vendor_id": 4098, "device_id": 29822, "driver": "radv"}
#
# sov-xa4: each entry below is a FULL message identity (hazard class + resource +
# pass/subpass), matched by the gate with exact whole-line equality. NEVER shorten
# an entry to the bare SYNC-HAZARD-* signature: a bare signature would allow-list
# every future hazard of that class on any resource, in any pass, forever.
#
# The three WRITE-AFTER-WRITE depth-stencil entries were removed when sov-ejz
# closed (barrier fix verified: 10 hazards -> 0 across debug and release runs).
# Vulkan loader warning observed during validation captures on this adapter.
Validation Warning: [ WARNING-vkGetDeviceProcAddr-device ] | MessageID = 0x22b1fbac

# Documentation reality audit — 2026-08-26

**Kind:** process audit
**Authority:** advisory — findings for the lead to disposition, not itself an authority source
**Status:** active
**Owner:** doc-reality-auditor
**Last verified:** 2026-08-26, tree at commit `8531d3cd` (working tree; `sov-lpj` in progress)

Phase-6-style sweep of every `.md` under `docs/`, `CLAUDE.md`, `AGENTS.md`, `README.md`, and
`.claude/agents/*.md` against current code, `bd`, and git history, per `sov-zoy`. `docs/archive/`
was checked only for correct provenance labeling (Kind/Status = historical, present-tense claims
absent), not deep-verified, per brief.

Known-live context taken as given, not re-flagged: `sov-dispatch-wedge-ab4` closed at `7e4b82f`
(re-verified: 42 passed/0 failed, serial); `sov-lpj` in flight, `request_multiplier` field
confirmed landed in `prototypes/src/types/recipe.rs:52`, `base_mod/companies.lua:40,582`,
`simulation/src/souls/goods_company.rs:23`; `docs/plan/iterations/RESUME.md` was corrected in the
working tree today and reads accurately (see Clean bill).

---

## Findings, ranked by blast radius

### 1. `docs/plan/charter-1.0.md:13-14` — STALE (highest blast radius: binding scope authority)

> says: "current code and cited substrate fact-sheets describe what exists; ratified
> specifications define how an in-scope system must behave; `br` defines task state."

> reality: `CLAUDE.md:45` — "`bd` (upstream beads 1.2.x) replaced the retired `br` fork on
> 2026-08-26." `br` is not installed; every live workflow doc (`CLAUDE.md`, `AGENTS.md`,
> `docs/plan/iterations/RESUME.md`) names `bd`.

who acts on it: every agent — this is the charter, `Authority: binding`, read before any scope
decision.

fix: replace `` `br` `` with `` `bd` `` at charter-1.0.md:14.

### 2. Five more active, present-tense docs still name `br` as live task-state authority — STALE

All are `Status: active`, non-archive, and were not caught by the earlier `bd`-cutover pass:

- `docs/README.md:20` — Authority-map table: `| What is being worked on now | `br` | Task state
  only |`
- `docs/reference/specifications/README.md:17` — "current code plus the cited substrate map
  controls claims about what exists, and `br` controls task state."
- `docs/decisions/README.md:21` — "specifications bind in-scope mechanism; `br` binds task
  state."
- `docs/research/fact-sheets/wave3-corpus.md:27,82` — "ratified specifications bind an in-scope
  mechanism; `br` owns task state" and "Reconstruct RESUME from `br`, verified commits..."
- `docs/plan/controlled-documentation-rewrite.md:54,143,159` — "`br` remains the only task-state
  authority," "Reconstruct... from current `br` state," "`doc-reality-auditor` checks docs, agent
  definitions, `br`..."

reality: same as #1 — `br` retired 2026-08-26, `bd` is the shared surface (`CLAUDE.md` §Task
tracking).

who acts on it: `docs/README.md` is the entrypoint to the whole authority map; `specifications/
README.md` gates every one of the 21 domain specs; `decisions/README.md` gates the (currently
empty) ADR mechanism. Any of these read cold sends an agent looking for a binary that no longer
exists.

Not flagged: `docs/plan/documentation-migration.md:18,100` and every `br` mention inside
`docs/explanation/research/*` — correctly past-tense/historical and labeled `Status: superseded`
/ `historical input` / `accepted research`. Those are provenance, not defects.

fix: `br` → `bd` at each cited line. `controlled-documentation-rewrite.md` additionally needs
a status check (see #3 — it may be entirely done, not just terminology-stale).

### 3. `docs/plan/controlled-documentation-rewrite.md` and `docs/research/fact-sheets/wave3-corpus.md` — ORPHANED / ASPIRATIONAL-AS-FACT

Both are `Status: active`, present-tense, and describe the Wave 1–3 documentation rewrite as a
plan still to be executed ("Wave 3 must proceed in this order...", "No generated file... can
establish scope upstream").

reality: `docs/plan/iterations/RESUME.md:5` — "**Status:** cutover complete — commit `b6381a5`;
parent closure recorded in `bd`." The rewrite this plan describes has shipped. Neither file is
linked from any other active doc (confirmed independently and by the existing
`docs/process/doc-survey-2026-08-26.md` orphan table, which flags `wave3-corpus.md` the same way).

who acts on it: nobody currently — that is the problem. An agent who finds either file via
`grep`/`glob` (not navigation) will read a live-sounding instruction to re-run a rewrite that's
already merged.

fix: mark both `Status: superseded`, point at `RESUME.md`, and drop or relocate `wave3-corpus.md`
under the same historical-input framing `documentation-architecture.md` and `study-brief.md`
already use correctly.

### 4. `docs/process/development-cycle.md:36-40` — WRONG (roster table contradicts every agent's own frontmatter)

> says: table lists `substrate-cartographer`, `kornai-economist`, `logistics-modeller`,
> `utilities-modeller`, `settlement-modeller` all at tier **opus**.

> reality: `grep "^model:" .claude/agents/*.md` — all 15 files, including those five, pin
> `model: sonnet`. Zero files declare `model: opus`.

who acts on it: every dispatch of a domain advisor — the lead reads this table to decide
cost/tier before dispatching, and the roster's own framing line ("sonnet implements, opus
reviews and advises") is falsified for the *advises* half by the files it names.

This is not a new discovery — `docs/process/review-2026-08-26-vs-swarmforge.md` (dated the same
day) already found and named this exact drift as its finding **F1a**, plus two more in the same
process layer (see #5, #6). I independently re-verified all three; they still hold.

fix: either repoint the five files to `model: opus` (cost decision — five standing opus advisors
is real spend on a one-person project) or fix the table to say sonnet and drop the "opus
reviews and advises" framing for advisors. Either way, make the table and the files agree.

### 5. `.beads/formulas/gate-chain.formula.toml` vs `docs/process/development-cycle.md:164-169` — WRONG (Phase 4 gate list)

> says (dev-cycle table): Phase 4 has four rows — `wiring-auditor`, `ledger-invariant-checker`
> (conditional on economy diffs), `reviewer`, domain advisor.

> reality: the formula (re-read in full) has exactly three steps — `wiring` → `domain` →
> `review` — with no ledger step at all, conditional or otherwise.

who acts on it: `bd mol pour gate-chain --var story=... ` — anyone pouring the formula for an
economy-touching story gets a chain silently missing the highest-yield gate
(`development-cycle.md`'s own text: "ledger-invariant-checker found 5 CONFIRMED findings...
where the general opus gate found 2... on the same seam").

Also already found as F1b in the swarmforge review; still unfixed.

fix: add a fourth, economy-conditional step to the formula (or a `gate-chain-economy` variant)
per the swarmforge review's own recommendation 1b.

### 6. `CLAUDE.md`'s `Executed-By:` trailer convention — WRONG (dead convention, documented as live)

> says (`CLAUDE.md` §Task tracking, "Adopted conventions"): "`BEADS_ACTOR=<agent-name>`... stamps
> comments, events, and — via the installed `prepare-commit-msg` hook — an `Executed-By:` trailer
> on commits."

> reality: `git log --format='%(trailers:key=Executed-By)' -60` returns 0 non-empty lines across
> the last 60 commits, including commits made after the hook's stated install date. `bd` 1.2.2's
> `prepare-commit-msg` hook does not append the trailer even with `BEADS_ACTOR` set.

who acts on it: every worker brief that sets `BEADS_ACTOR` believing it produces a commit-level
audit trail — it doesn't. Also F1c in the swarmforge review, with the same root cause identified
there (bd 1.2.2 recovery-release gap) and a concrete fix proposed (a project-local
`prepare-commit-msg` hook, or delete the convention).

fix: either install the three-line hook the swarmforge review specifies, or strike the
`Executed-By` claim from `CLAUDE.md`.

### 7. `docs/process/review-2026-08-26-vs-swarmforge.md` — ORPHANED (the audit that found #4–#6 is itself unreachable)

Not linked from `development-cycle.md`, `CLAUDE.md`, or any other active doc — confirmed by grep
and independently flagged by `docs/process/doc-survey-2026-08-26.md`'s orphan table. Its Part 3
recommendations (fix the tier table, add the ledger step, fix or drop the trailer, point
doc-reality-auditor at the process layer itself) are unactioned three findings deep into a
document nobody reaches by navigation.

fix: link it from `development-cycle.md` (e.g., a "known process-layer drift" pointer) or fold
its unresolved recommendations into an issue and archive the document once actioned.

### 8. `docs/plan/iterations/evidence/current-regression-inventory.md` — STALE, self-disclosed

> says: lists 26 `REGRESSION-*` rows as "every test currently listed by the serial `simulation`
> test binary."

> reality: `cargo test -p simulation -- --test-threads=1` now reports **42 passed**, and
> `build_evidence.py --check` fails outright: "differs from deterministic evidence output."

This is not a silent drift — `RESUME.md:54-56` already discloses it explicitly ("the evidence
`current-regression-inventory.json` and the roadmap were not regenerated with it; that
regeneration is owed at the next Phase 6 wrap") and this **is** that wrap. Reporting it as a
finding rather than folding it into the clean bill because the doc's own header claims
`Status: informational` with no visible staleness flag — a reader who doesn't also read
RESUME.md would trust the 26-row table at face value.

fix: run `build_evidence.py` (not `--check`) to regenerate the inventory and roadmap from the
current 42-test suite, then re-run `--check` to confirm.

### 9. `docs/decisions/README.md` — minor structural gap, not a false claim

Describes a live ADR lifecycle (draft → accepted → superseded → archived) for a directory that
contains only the README and a template pointer — zero actual decisions recorded. The only ADRs
in the repo are the 17 frozen ones under `docs/archive/bevy-track/decisions/`, for the discarded
track. Not WRONG (the process description is accurate), but worth flagging: a mechanism this
project asserts as its ratification path for decisions has never been used post-fork. Already
caught independently by `docs/process/doc-survey-2026-08-26.md` oddity #1.

fix: none required unless the lead wants ADRs actually exercised; otherwise no action.

### 10. Agent-file header convention — NOT a finding (false alarm ruled out)

Checked whether `.claude/agents/*.md` follow the `docs/` Kind/Authority/Status/Owner convention.
They do not — but that convention is scoped to `docs/` by `docs/README.md`'s own text; agent
files instead use YAML frontmatter (`name`, `description`, `tools`, `model`, `effort`, `memory`,
`color`), which all 15 files carry consistently. Two different, both-valid conventions; not
drift.

---

## Clean bill — checked and found accurate

- `docs/plan/iterations/RESUME.md` — every live claim verified: `sov-dispatch-wedge-ab4` closed
  at `7e4b82f` (bd confirms CLOSED, reason cites the commit); serial suite is 42 passed / 0
  failed (re-ran live, matches); `sov-lpj` is IN_PROGRESS and front of `bd ready` order (both
  confirmed); `request_multiplier` landed exactly where sov-lpj's acceptance criteria specify.
  This file was corrected today and it holds.
- `docs/plan/iterations/requirements/` and `docs/plan/iterations/extract/` — both pass their own
  `--check`: 149 STORY rows / 21 live requirements / 21 stable identities; 21 validated
  requirements.
- `docs/generated/iterations/roadmap.md` — passes its own `--check`; its totals (21 requirements,
  107 planned, 0 implemented) match `docs/plan/iterations/evidence/coverage.md` exactly, row for
  row.
- `docs/reference/architecture/substrate.md` — every cited file:line spot-checked
  (`init.rs:52-109`, `init.rs:111-130`, `lib.rs:244-270`, `lib.rs:359-448`) matches its
  classification language; no overclaim found.
- `README.md` (repo root) — accurate throughout: fork date, licence, layout table, economy
  pillars all match code and CLAUDE.md.
- `CLAUDE.md`, `AGENTS.md` — accurate on the `bd`/`br` cutover itself (both correctly say `bd`
  replaced `br` on 2026-08-26) and on the serial-test-parallelism fix; the residual `br` staleness
  lives in *other* docs those two didn't touch, not in themselves.
- `docs/reference/specifications/README.md` — self-consistent on draft-vs-ratified status (all 21
  specs correctly `draft`, not "ratified" as CLAUDE.md's shorthand implies elsewhere — but
  CLAUDE.md's own line 15 already hedges with "after each specification is ratified," so this is
  not a contradiction, just something to watch as specs actually ratify).
- `sov-361` (open, not in `bd ready`) — correctly blocked on `sov-lpj`, not a stale/orphaned
  ticket; the lifetime-warning class it targets still exists in the build.
- Bevy/`src/sim/`/`src/game/` mentions outside `docs/archive/`: all seven hits (in
  `development-cycle.md`, `doc-reality-auditor.md`, `art-direction.md`, and three
  `docs/explanation/research/*` files) are past-tense cautionary references to the historical
  incidents, not live instructions pointing at a discarded path. No agent is told to go read
  `bevy.md`.
- `market.rs` — the historical "human buyer owns no building" comment (this agent's own origin
  story) is no longer present in the current file; `human.rs:275` calls `set_owner(house, soul)`
  as expected. Already fixed, not a live defect.
- The 21 domain specifications under `docs/reference/specifications/` are, per an independent
  corpus survey (`docs/process/doc-survey-2026-08-26.md`), the most consistently headered cluster
  in the corpus (4/4 header fields, all 21 files) — spot-checked and confirmed.

## Orphans

Cross-checked against `docs/process/doc-survey-2026-08-26.md`'s basename-link survey (which I
independently re-derived and confirm is accurate). Of its 58 orphans, 56 are expected-by-design
(archive EPIC/PAR files meant to be read via their own INDEX, plus two unlinked templates meant
to be copied, not linked). Two are live-tree problems, both discussed above:

- `docs/process/review-2026-08-26-vs-swarmforge.md` — active, orphaned, and contains three unfixed
  findings about this exact process (#4–#6 above).
- `docs/research/fact-sheets/wave3-corpus.md` — active, orphaned, and describes a rewrite that has
  already shipped (#3 above).

---

## Summary for the lead

| Verdict | Count |
|---|---|
| STALE | 5 (charter `br`, 4 more active `br` docs collapsed as one finding class + regression inventory) |
| WRONG | 2 (roster tier table, gate-chain formula) |
| ASPIRATIONAL-AS-FACT / ORPHANED-STALE | 2 (controlled-documentation-rewrite.md, wave3-corpus.md) |
| ORPHANED (content fine, unreachable) | 1 (the swarmforge review itself) |
| Minor / no-action | 2 (empty ADR register, agent-header convention false alarm) |

Ten items reported per the brief; ranked above by blast radius. The single highest-value fix is
#1 (`charter-1.0.md`'s `br` reference — it's the binding-scope document). The most consequential
*unfixed-and-already-known* problem is #4–#7 together: this project's own process-layer
self-audit (the swarmforge review) found real drift three days ago and nobody linked it into the
navigation graph, so its fixes never landed.
